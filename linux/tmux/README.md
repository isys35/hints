<h1>tmux</h1>

Установка:

```shell
sudo apt-get install tmux
```

Открытые сессии:

```shell
tmux ls
```

Разделить окно по горизонтали:

```shell"
CTRL+B “
```

Разделить окно по вертикали:

```shell
CTRL+B %
```

Закрыть окно: 

```shell
CTRL+D
```